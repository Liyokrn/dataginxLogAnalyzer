apiVersion: v1
kind: Service
metadata:
  name: loganalyzer-frontend
  namespace: default
  labels:
    app: loganalyzer-frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: loganalyzer-frontend
